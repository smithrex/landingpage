<p id="notice"><%= notice %></p>

<h1>Orden Proyectos</h1>

<table>
  <thead>
    <tr>
      <th>Orden</th>
      <th>Proyecto</th>
      <th>Instrucciones</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @orden_proyectos.each do |orden_proyecto| %>
      <tr>
        <td><%= orden_proyecto.orden_id %></td>
        <td><%= orden_proyecto.proyecto_id %></td>
        <td><%= orden_proyecto.instrucciones %></td>
        <td><%= link_to 'Mostrar', orden_proyecto %></td>
        <td><%= link_to 'Editar', edit_orden_proyecto_path(orden_proyecto) %></td>
        <td><%= link_to 'Eliminar', orden_proyecto, method: :delete, data: { confirm: 'Estas seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo Orden Proyecto', new_orden_proyecto_path %>
